# wordpress-docker

## Pre
 - Install Docker
 - Install Docker Compose

## Run
 - `docker-compose up`

## Check Installation
 - `localhost:8000` ~> wordpress dashboard
 - `localhost:8080` ~> phpmyadmin page
